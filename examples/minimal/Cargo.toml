[package]
name = "activelog-minimal"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
activelog = { path = "../../packages/activelog" }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
