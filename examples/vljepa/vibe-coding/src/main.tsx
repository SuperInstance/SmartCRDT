/**
 * Vibe Coding Demo - Main Entry Point
 *
 * VL-JEPA "Show, Don't Tell" UI Editing Demo
 * Demonstrates the complete workflow: capture → plan → approve → execute
 */

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './styles/vibe-coding.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
